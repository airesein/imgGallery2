{
  "name": "img-gallery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-catalog.mjs && node scripts/generate-sitemap.mjs",
    "dev": "npm run generate && vite",
    "build": "npm run generate && vite build && node scripts/generate-static.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "vue": "^3.4.0",
    "js-yaml": "^4.1.1",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.4.0"
  }
}
